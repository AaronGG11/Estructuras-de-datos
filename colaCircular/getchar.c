#include<stdio.h>

int main(int argc, char *argv[]){

	putchar(getchar());
	putchar(getchar());
	putchar(getchar());

	printf("\n");

	// El buffer del teclado tambien incluye al enter
	// checar crea, ruleta: apuesta, gira, resuñltado


	return 0;
}